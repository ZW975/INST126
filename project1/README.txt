This is a simple calculator.
calclulation including add, minus, times, divide, factorial and power.

It starts with a menu output.
Then check the input to see whether the input number is right using a while loop.

The addition and multiply part can accept more than 2 numbers.
They use while loop til the input is 0.

While subtraction and minus takes 2 numbers only.
Simple input and calculation only.

Factorial and power are simple inputs as well.

The last part is only a print that shows the result.